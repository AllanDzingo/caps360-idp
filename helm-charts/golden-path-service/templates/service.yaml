apiVersion: v1
kind: Service
metadata:
  name: {{ include "golden-path-service.fullname" . }}
  labels:
    {{- include "golden-path-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "golden-path-service.selectorLabels" . | nindent 4 }}
